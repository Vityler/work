"use strict";

let number = 4;

console.log(4/0);       //числа, кавычки не нцжны
console.log('string'*8);

const person = "alex";//строки в кавычки, " или '

const bool = true;

console.log(something);//null

let und;
console.log(und);//udefined

const obj ={//объект, имеет ключ и значение
    name:'John',
    age:25,
    isMarried:false
};
//console.log(obj.name);
console.log(obj['name']);

let arr = ['plum.png', 'orange.jpg',6,[], {}, 'apple.bmp'];// массив, ключ - порядковый номер от 0
console.log(arr[2]);//значение в квадратных скобках
